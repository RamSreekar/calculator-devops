---
- name: Pull docker image
  hosts: all
  tasks:

    - name: Start docker service
      service:
        name: docker
        state: started

    - name: pull docker image
      shell: docker pull jvrsreekar/calculator_devops

    - name: run the container
      shell: docker run -it -d jvrsreekar/calculator_devops